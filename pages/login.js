import Layout, { appName } from '../components/sitewide/layout'


export default function Login({ lang }) {
  return (
    <Layout lang={lang}>
      <main>
        <form>
          
        </form>
      </main>
    </Layout>
  )
}